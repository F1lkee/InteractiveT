<Window x:Class="InteractiveTWPF.StudentDashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InteractiveTWPF"
        mc:Ignorable="d"
        Title="Личный кабинет ученика" 
    Height="700" Width="1000"
    WindowStartupLocation="CenterScreen"
    Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Шапка с прогрессом -->
        <Border Background="{StaticResource AccentBrush}" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock x:Name="WelcomeTextBlock"
                          Text="Добро пожаловать!"
                          FontSize="24" 
                          FontWeight="Bold"
                          Foreground="White"/>
                    <TextBlock x:Name="ClassTextBlock"
                          Text="Класс: 11А"
                          FontSize="14"
                          Foreground="#DDFFFFFF"
                          Margin="0,5,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" 
                    Content="Выйти"
                    Click="LogoutButton_Click"
                    Background="#33FFFFFF"
                    Foreground="White"
                    Padding="20,10"
                    BorderThickness="0"/>
            </Grid>
        </Border>

        <!-- Основное содержимое -->
        <ScrollViewer Grid.Row="1" Margin="20">
            <StackPanel>
                <TextBlock Text="Доступные тесты" 
                      FontSize="20"
                      FontWeight="SemiBold"
                      Foreground="{StaticResource TextPrimaryBrush}"
                      Margin="0,0,0,20"/>

                <ItemsControl x:Name="TestsItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}"
                                              FontSize="18"
                                              FontWeight="SemiBold"
                                              Foreground="{StaticResource TextPrimaryBrush}"/>
                                        <TextBlock Text="{Binding SubjectName}"
                                              Foreground="{StaticResource TextSecondaryBrush}"
                                              Margin="0,5,0,0"/>
                                        <TextBlock Text="{Binding Description}"
                                              TextTrimming="CharacterEllipsis"
                                              Foreground="{StaticResource TextSecondaryBrush}"
                                              Margin="0,5,0,0"/>

                                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                            <TextBlock Text="⏱" Margin="0,0,5,0"/>
                                            <TextBlock Text="{Binding TimeLimitDisplay}"
                                                  Foreground="{StaticResource TextSecondaryBrush}"/>
                                            <TextBlock Text="   📝" Margin="10,0,5,0"/>
                                            <TextBlock Text="{Binding QuestionsCount, StringFormat='{}{0} вопросов'}"
                                                  Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <Button Grid.Column="1"
                                        Content="Начать"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        VerticalAlignment="Center"
                                        Margin="20,0,0,0"
                                        Click="StartTestButton_Click"
                                        Tag="{Binding Id}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="История прохождения" 
                      FontSize="20"
                      FontWeight="SemiBold"
                      Foreground="{StaticResource TextPrimaryBrush}"
                      Margin="0,30,0,20"/>

                <DataGrid x:Name="HistoryDataGrid"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     Background="{StaticResource CardBrush}"
                     BorderThickness="0"
                     RowBackground="{StaticResource CardBrush}"
                     AlternatingRowBackground="#FFF8F9FA"
                     GridLinesVisibility="None">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Тест" Binding="{Binding TestTitle}" Width="*"/>
                        <DataGridTextColumn Header="Дата" Binding="{Binding StartedAt, StringFormat=dd.MM.yyyy HH:mm}" Width="Auto"/>
                        <DataGridTextColumn Header="Результат" Binding="{Binding ScoreDisplay}" Width="Auto"/>
                        <DataGridTextColumn Header="Статус" Binding="{Binding StatusDisplay}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
